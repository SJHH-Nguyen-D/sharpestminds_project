import numpy as np 
import pandas as pd 
import beautifulsoup4, os, requests, plotly, argparse, sys

from utils.constants import *
from utils.utils import *
from utils.webscraper import *

# parser = ArgumentParser()
# parser.add()

data = data_loader(os.path.join(ROOT_DIR, "dataset"))

def main():
	print("Things in the main script")


if __name__ == "__main__"
	parser = argparse.ArgumentParser(description="This script does a thing")
	main()